# ─────────────────────────────────────────────────────────────────────────────
# EtherBrain — configuración local
# Copia este archivo como .env y rellena tus valores.
# El .env NUNCA debe subirse al repositorio (ya está en .gitignore).
# ─────────────────────────────────────────────────────────────────────────────

# ── LLM ──────────────────────────────────────────────────────────────────────
# LLM_TYPE  : openai | anthropic | gemini | bedrock
# LLM_URL   : URL BASE del proveedor — el codec añade el path automáticamente
# LLM_TOKEN : API key (vacío para Ollama local)
# LLM_MODEL : nombre del modelo
#
# Proveedor         LLM_TYPE    LLM_URL (solo la base — sin path)
# ───────────────────────────────────────────────────────────────────────────
# Anthropic Claude  anthropic   https://api.anthropic.com
# OpenAI            openai      https://api.openai.com
# Groq              openai      https://api.groq.com
# Deepseek          openai      https://api.deepseek.com
# Mistral           openai      https://api.mistral.ai
# Cerebras          openai      https://api.cerebras.ai
# OpenRouter        openai      https://openrouter.ai
# Ollama local      openai      http://localhost:11434
# Google Gemini     gemini      https://generativelanguage.googleapis.com
# AWS Bedrock       bedrock     https://bedrock-runtime.{region}.amazonaws.com

# ── Activo — cambia al proveedor que uses ────────────────────────────────────

# Anthropic Claude
LLM_TYPE=anthropic
LLM_URL=https://api.anthropic.com
LLM_TOKEN=sk-ant-
LLM_MODEL=claude-haiku-4-5

# ── Alternativas (descomenta la que uses) ─────────────────────────────────────

# OpenAI
# LLM_TYPE=openai
# LLM_URL=https://api.openai.com
# LLM_TOKEN=sk-
# LLM_MODEL=gpt-4o-mini

# Groq (cuenta gratuita en console.groq.com)
# LLM_TYPE=openai
# LLM_URL=https://api.groq.com
# LLM_TOKEN=gsk_
# LLM_MODEL=llama-3.3-70b-versatile

# Cerebras
# LLM_TYPE=openai
# LLM_URL=https://api.cerebras.ai
# LLM_TOKEN=csk-
# LLM_MODEL=gpt-oss-120b

# Deepseek
# LLM_TYPE=openai
# LLM_URL=https://api.deepseek.com
# LLM_TOKEN=sk-
# LLM_MODEL=deepseek-chat

# Mistral
# LLM_TYPE=openai
# LLM_URL=https://api.mistral.ai
# LLM_TOKEN=
# LLM_MODEL=mistral-large-latest

# OpenRouter (enruta a cualquier modelo — ver openrouter.ai/models)
# LLM_TYPE=openai
# LLM_URL=https://openrouter.ai
# LLM_TOKEN=sk-or-
# LLM_MODEL=anthropic/claude-opus-4-5

# Ollama local (sin token)
# LLM_TYPE=openai
# LLM_URL=http://localhost:11434
# LLM_TOKEN=
# LLM_MODEL=llama3.2

# Google Gemini
# LLM_TYPE=gemini
# LLM_URL=https://generativelanguage.googleapis.com
# LLM_TOKEN=AIzaSy
# LLM_MODEL=gemini-2.0-flash

# AWS Bedrock (requiere credenciales AWS en el entorno)
# LLM_TYPE=bedrock
# LLM_URL=https://bedrock-runtime.us-east-1.amazonaws.com
# LLM_MODEL=anthropic.claude-3-5-sonnet-20241022-v2:0

# ── Sesiones ──────────────────────────────────────────────────────────────────
SESSION_DIR=/tmp/etherbrain-sessions

# ── Logging ───────────────────────────────────────────────────────────────────
LOG_LEVEL=INFO

# ── Knowledge base faiss-poc (opcional) ──────────────────────────────────────
# FAISS_BASE_URL=https://51.161.11.83:8443
# FAISS_EMAIL=tu@email.com
# FAISS_PASSWORD=tu_password
# FAISS_SKIP_TLS_VERIFY=true
#
# Namespace por defecto para knowledge_search.
# Si está definido, el modelo no necesita especificarlo en cada búsqueda.
# FAISS_DEFAULT_NAMESPACE=mi-namespace
